# Copy to .env (loaded automatically in dev via dotenvy):
#   cp .env.example .env
#
# Twitch API (optional — seeds work without these)
TWITCH_CLIENT_ID=your_client_id
TWITCH_CLIENT_SECRET=your_client_secret

# Optional salt for hashing voter IPs (production recommended)
IP_HASH_SALT=
